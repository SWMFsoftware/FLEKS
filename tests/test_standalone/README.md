# Standalone FLEKS Test

This directory contains a minimal `PARAM.in` for testing FLEKS in standalone mode.
The test is executed as part of the CI workflow to ensure that the core PIC and solver logic remains functional independent of SWMF.
