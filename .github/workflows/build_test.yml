# This workflow has been split into:
# 1. swmf_test.yml (for SWMF integrated tests)
# 2. standalone_test.yml (for FLEKS standalone tests)
#
# Please use or edit those files instead of this one.

name: Legacy Build Test Workflow (Replaced)
on: []
jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is legacy and has been replaced by swmf_test.yml and standalone_test.yml"
